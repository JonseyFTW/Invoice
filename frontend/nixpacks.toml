providers = ["node"]

[variables]
NODE_VERSION = "20"
NODE_ENV = "production"

[phases.install]
cmd = "npm ci --no-audit --no-fund"

[phases.build]
cmd = "npm run build"

[start]
cmd = "npx serve -s build -l $PORT"